# FuckYourCompany · 社保公积金欠缴计算器

> 社保公积金欠缴计算器 · 守护你的每一分钱  
> 当前版本：**V 0.0.2**

单文件 HTML 应用，双击即可运行。支持工资记录、社保基数、公积金基数的逐月录入与欠缴计算，支持导出 PDF / 图片 / 保存网页副本。

---

## 一、功能更新

### 基本设置
- 选择城市（上海/北京/广州/深圳/杭州/成都/南京/武汉/自定义）
- 入职日期 / 计算结束日期
- 自定义社保 / 公积金费率面板
- 入职日期填写后自动生成一条工资、社保基数、公积金基数记录
- **基数核定规则面板**：职工入职类型（普通/新参加工作/新调入）、基数核定方式（自动推算/按当月收入/完全手动）、基数调整月
- **查看范围**：全部 / 仅社保 / 仅公积金（已移至基数核定规则下方）
- **城市联动提示**：切换城市后，入离职提示、基数核定政策说明、调整月信息均自动跟随城市变化

### 记录管理
- **工资收入明细**：每月收入拆分为基本工资、奖金、津贴、补贴、其他 5 项，实时自动汇总合计；奖金/津贴/补贴/其他默认显示 0
- 工资记录、社保基数记录、公积金基数记录均按日期从旧到新排序
- **社保基数新增**：默认填充当前城市最低标准（如上海 7460）
- **公积金基数新增**：默认填充当前城市公积金最低基数（如上海 2690）
- 单条基数记录时，计算始终按该基数计算（不再把最后一个月按工资基数走）
- 增删记录后自动同步状态

### 计算与结果
- 点击「计算欠缴金额」后：
  - 汇总卡片：个人欠缴 / 公司欠缴 / 合计
  - 分类汇总对比表
  - 逐月明细表（全部展开，无滚动限制）
  - 逐月明细中连续相同月份合并为一行，用 `; ` 分隔
  - **核定基数推算**：按上海政策自动推算各月应缴基数（上一年度月平均工资），支持新参加工作/新调入职工的特殊基数规则；手动填写的社保/公积金基数记录优先级最高
  - 逐月明细显示收入总额、应缴基数（标注"推算"或手动）、实缴基数
- 查看范围切换时，欠缴金额仅显示对应范围（仅社保→只社保，仅公积金→只公积金）
- 总缴对比图、月度差额趋势图位于「计算欠缴金额」按钮下方

### 导出功能
- 导出 PDF：A4 分页，页间 15px 重叠避免数字跨页被切
- 导出图片（JPG）：html2canvas 截图，自动裁掉底部多余空白
- 保存网页：以 `FuckYourCompany_YYYY-MM-DD.html` 命名，内嵌完整状态 + 图表快照 + 结果 HTML，打开即可查看完整内容，不依赖 localStorage
- 导出时页面无闪烁、无拉宽变形

---

## 二、版本日志

### V 0.0.2
- **工资收入明细扩展**：每月收入拆分为基本工资、奖金、津贴、补贴、其他 5 项，实时自动汇总；奖金/津贴/补贴/其他默认显示 0
- **基数核定规则面板**：支持职工入职类型（普通/新参加工作/新调入）、基数核定方式（自动推算/按当月收入/完全手动）、新参加工作/新调入子选项、基数调整月
- **核定基数推算引擎**：按政策自动推算各月应缴基数（上一年度月平均工资），支持新参加工作（第二个月工资/入职以来月均）和新调入（调入当月工资/调入后月均）特殊规则，手动记录优先级最高
- **城市联动提示**：切换城市后，入离职提示、基数核定政策说明、调整月信息均自动跟随城市变化（不再硬编码"上海"）
- **社保/公积金基数默认值**：新增记录时自动填充当前城市的最低标准值
- **查看范围移位**：从城市选择下方移至基数核定规则面板下方
- **上海实例对比卡片**：新增奖金/津贴/补贴/其他收入输入（折叠式），月收入总额自动同步为全额基数
- 逐月明细显示收入总额、核定基数来源标注（推算/手动）
- 旧数据自动迁移兼容（localStorage + 内嵌快照双路径）

### V 0.0.1
- 初始版本
- 基本设置 / 工资记录 / 社保基数 / 公积金基数录入与排序
- 欠缴金额计算（个人 / 公司 / 合计）
- 逐月明细全部展开，连续相同月份合并显示
- 总缴对比图 + 月度差额趋势图
- 导出 PDF / 导出图片 / 保存网页
- 导出时数字完整显示（无底部裁切）、无页面闪烁、无多余空白
- 保存网页不依赖缓存，内嵌完整状态与图表快照

---

## 三、经验教训

### 1. html2canvas 导出数字底部被裁切
**现象**：导出的图片中，输入框内数字的底部约 1/4 被切掉。  
**根因**：Tailwind 的 `overflow-hidden`（用于卡片圆角）+ html2canvas 渲染 `<input>` 比 Chrome 原生高 2-3px，导致最后一行内容的下行像素被裁。  
**解决**：
- 不在真实 DOM 上改样式（避免页面闪烁），只在 html2canvas 的 `onclone` 回调里对克隆 iframe 打 inline style patch
- `<section>`：`overflow: visible` + `padding-bottom +10px`
- `<input>`：`box-sizing: content-box` + 上下对称各 `+4px` padding + `vertical-align: top` + `line-height: 1.6`
- 文本容器：`line-height: 1.66` + `padding-bottom: 2px`

### 2. 导出前页面「拉宽 / 拉长」闪烁
**现象**：点击导出时页面瞬间变形。  
**根因**：在真实 DOM 上切 `.exporting` class（main 宽度从 ~1152px 拉到 1440px）触发整页重排，patchSubtree 的测量在重排前完成，对不上重排后的布局。  
**解决**：真实 DOM 完全不动，所有 patch 只在离线克隆 iframe 内执行。iframe 宽度用真实 DOM 实测的 `preflightW`（非硬编码 1440），保证 1:1 布局。

### 3. 导出图片底部多出一大段空白
**现象**：导出的 JPG / PDF 底部有数百像素纯背景色空白。  
**根因**：传了 `width` 但没传 `height` 给 html2canvas，画布高度被强行填到 `windowHeight`。  
**解决**：
- 不传 `width` / `height`，让 html2canvas 按克隆节点实际渲染尺寸出图
- 像素级兜底：`_trimCanvasBottomBlank` 从最后一行往上扫描，找到最后一个有非背景色像素的行，裁到 `lastNonBgRow + 2`

### 4. 保存网页后打开内容为空
**现象**：保存的 HTML 打开后基本设置参数、图表、结果区全部为空。  
**根因**：
- `outerHTML` 只序列化 HTML attribute，不序列化用户在 input/select 中实时编辑的 `.value`
- Chart.js 画的内容在 canvas 显存里，`<canvas>` 标签本身是空壳
- 保存的 HTML 依赖 localStorage 读数据  
**解决**：
- 保存前 `_serializeAllFormValuesToAttrs()` 把所有 `.value` 写回 attribute
- `_materializeChartsWithImg()` 用 `canvas.toDataURL()` 把图表转为 `<img>`
- 把 state + chartsHtml + resultsHtml 打包成 JSON 注入 `<script type="application/json">`
- 打开时检测到内嵌 JSON 就直接用，跳过 localStorage

### 5. 计算逻辑：单条基数记录
**问题**：只有一条社保 / 公积金基数记录时，计算把最后一个月按工资基数走。  
**解决**：单条记录时始终按该基数计算，不再回退到工资基数。

### 6. 查看范围切换
**问题**：切换查看范围后欠缴金额未更新。  
**解决**：切换时重新计算并仅显示对应范围的欠缴金额。

### 7. 旧数据格式迁移：salaries 字段从 amount 到 5 项拆分
**现象**：V0.0.1 的 localStorage 中 `state.salaries` 每条记录只有 `{date, amount}`，升级到 V0.0.2 后新增 `baseSalary/bonus/allowance/subsidy/other/totalIncome` 字段，旧数据缺少这些字段导致 UI 和计算异常。  
**根因**：`readState()` 直接读取新字段名，遇到旧记录返回 undefined，`parseFloat(undefined) || 0` 把"未填"和"0"混为一谈。  
**解决**：
- 新增 `_migrateStateIfNeeded()` 函数，在 `loadState()` 末尾和内嵌快照恢复路径**都**调用
- 旧记录 `{amount}` → `{baseSalary: amount, bonus:0, allowance:0, subsidy:0, other:0, totalIncome: amount}`
- 同时给 state 补全所有新增的默认字段（`employeeType/siBaseRule/fundBaseRule` 等），避免 `undefined` 蔓延到计算链路

### 8. 城市切换后政策提示文本未跟随更新
**现象**：从上海切换到杭州后，入离职提示仍显示"上海入离职当月..."，基数核定规则面板仍显示"按上海政策"。  
**根因**：3 处文案硬编码"上海"，城市切换监听器只调用了 `updateCityTip()`（仅更新城市选择器下方的简要提示）。  
**解决**：
- 将 3 处硬编码"上海"替换为带 id 的 `<span>`（`cityRule-warn`/`cityRule-policy`/`cityRule-policy2`/`cityRule-adjMonth`/`cityRule-adjMonth2`）
- 新增 `updateCityRuleTexts()` 函数，从 `CITY_DATA[city].adjMonth` 动态读取调整月并更新文本 + 同步 `state.baseAdjustMonth`
- 城市 change 监听器和初始化阶段（loadState+restoreUI 之后）都调用一次

### 9. 基数推算引擎：上一年度月均工资的周期定位
**现象**：上海每年 7 月调整基数，2024-07~2025-06 周期的"上一年度"是 2023 自然年（1-12 月），而非 2024。  
**根因**：直接用 `month 的年份 - 1` 会导致 2024-06 的上一年度被错误计算为 2023（实际应是 2022，因为 2024-06 还在 2023-07~2024-06 周期内，上一年度=2022）。  
**解决**：
- `_getPrevYearForBase(month)` 判断：若月份 >= 调整月(7)，上一年度 = 当前年 - 1；否则上一年度 = 当前年 - 2
- 例如 2024-07 → 2023，2024-06 → 2022

### 10. 社保/公积金基数新增记录默认值为 0
**现象**：点击"+ 添加"社保/公积金基数时，输入框为空，用户需要手动查找当前城市的最低标准值。  
**根因**：`addSIBaseRow()` / `addFundBaseRow()` 未设置默认值，`value=""`。  
**解决**：
- `addSIBaseRow()`：amount 为空时调用 `getCityBaseLimits(state.city, date)` 取 `.lower`
- `addFundBaseRow()`：amount 为空时取 `FUND_MIN_BASES[state.city]`，无匹配城市回退 2500
- 注意仅影响新添加的空白记录，restoreUI 恢复已有记录时传入的 amount 不为空，不受影响
